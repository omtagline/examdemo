import { CharTostrPipe } from './char-tostr.pipe';

describe('CharTostrPipe', () => {
  it('create an instance', () => {
    const pipe = new CharTostrPipe();
    expect(pipe).toBeTruthy();
  });
});
